# Fabrication Cost Estimator (Streamlit)

Estimate duct fabrication cost by cost-center, export PDF/Excel, and view an S-curve.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
